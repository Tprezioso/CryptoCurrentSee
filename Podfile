# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CryptoCurrentSee' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
    pod 'Alamofire', '~> 4.3'
  # Pods for CryptoCurrentSee

  target 'CryptoCurrentSeeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'CryptoCurrentSeeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
